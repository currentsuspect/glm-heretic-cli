# glm-heretic-cli

The runnable project now lives in `glm-cli/`.

Quick start:

```bash
./glm --install
```

Keep using the same root commands:

```bash
./glm
./glm --agent
./glm --serve
```

Internal layout:

```text
glm-cli/
  AGENTS.md
  README.md
  scripts/glm
  src/glm_cli/
```
